remote-control:
    # Local control socket only (unbound-control reload / flush_zone / stats).
    # Not a network listener - a unix socket the host's own tooling drives.
    control-enable: yes
    control-interface: /run/unbound.ctl
