server:
    # Query + reply logging on. Volume is high, but these lines are shipped to
    # the SIEM, where DNS becomes a detection surface: NXDOMAIN bursts, lookups
    # for known-bad domains, and resolution of names no internal host should be
    # asking for all show up here first.
    verbosity: 2
    log-queries: yes
    log-replies: yes
